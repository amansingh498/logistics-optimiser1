services:
  - type: web
    name: logi-opt-api
    runtime: python
    rootDir: backend/api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: CORS_ORIGINS
        sync: false
